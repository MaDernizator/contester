# Contester Frontend

Minimal React frontend MVP for local development and manual testing.

## Run locally

```bash
npm install
npm run dev

The Vite dev server proxies /api requests to:

http://127.0.0.1:8000

If your backend runs on a different port, update vite.config.ts.